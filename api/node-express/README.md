# Node.js / Express

## Install

```sh
npm install express
```

## Run

```sh
export JUNGLE_GRID_API_KEY=jg_...
node server.mjs
```

The server listens on port `3000` by default and exposes:

- `POST /jobs/estimate`
- `POST /jobs`
